#! /bin/sh
#
# Erase migrations and start 

# set user and group id of the current user
. ./bin/get_local_env.dev


echo "erase migration files"
rm -rf */migrations/*

#  todo: get a list of all apps
for i in core app project resume team user
do
    echo "process $i"
  touch $i/migrations/__init__.py
done

./bin/migrate.dev

